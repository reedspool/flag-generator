export type Component = {
    componentId: string;
    componentIndex: string;
};

export type FlagDimensions = {
    width: number;
    height: number;
};

export type NumericAttr = number | string;

export type ColorableSVG = {
    fill?: string;
    stroke?: string;
};
